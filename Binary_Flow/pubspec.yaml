name: binary_reading_time
description: Calculate binary reading time from file size
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.0.0
  intl: ^0.19.0
